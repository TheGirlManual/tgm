/*
 * EpisodeDetailPage Messages
 *
 * This contains all the text for the EpisodeDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EpisodeDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EpisodeDetailPage container!',
  },
});
