/*
 * EpisodesPage Messages
 *
 * This contains all the text for the EpisodesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EpisodesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EpisodesPage container!',
  },
});
