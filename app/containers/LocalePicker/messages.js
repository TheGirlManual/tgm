/*
 * LocalePicker Messages
 *
 * This contains all the text for the LocalePicker container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LocalePicker';

export default defineMessages({
  en: {
    id: `${scope}.en`,
    defaultMessage: 'English',
  },
  es: {
    id: `${scope}.es`,
    defaultMessage: 'Español',
  },
});
